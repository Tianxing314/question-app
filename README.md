# Instruction 

## start question-app-server

`cd` to question-app-server directory

run `npm install`

run `node server.js`

## start question-app

`cd` to question-app directory

run `npm install`

run `npm start`
